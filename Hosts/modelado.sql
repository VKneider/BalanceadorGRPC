
create database if not exists balanceador;

CREATE TABLE table1 (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL

);


CREATE TABLE table2 (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL
);


CREATE TABLE table3 (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL
);

